# [blog.skydown](https://blog.skydown.co.kr)

* SPA + RESTFul API
* vue(vuex + vue-router + vuex)

``` bash
# webpack dev server (http://localhost:8080)
$npm run dev

# webpack build
$npm run build
```